<Window x:Class="FPECallLog.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window1"
        Height="451"
        Width="333" Loaded="Window_Loaded">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" Height="261" HorizontalAlignment="Left" Name="CallGrid" VerticalAlignment="Top" Width="278" ItemsSource="{Binding}" />
        <StackPanel Height="153" HorizontalAlignment="Left" Margin="0,259,0,0" Name="stackPanel1" VerticalAlignment="Top" Width="280">
            <Button Height="36" />

        </StackPanel>
    </Grid>
</Window>